#pragma once
//==========================================================================================
//
// ISystem (システム実体の土台)
//
// ECS基盤が知っているのは「システムという実体が1つある」ということだけ。
// 何をどう登録するか(Init の引数に何を渡すか)は上位層が決めるので、
// ここには初期化の口を置かない。
//
// SystemManager はタスクを実行するだけで、システム実体には触らない。
// 実体を持っているのは、遅延初期化などで実体側に状態を置きたい場合に
// 寿命を保証するため。
//
//==========================================================================================

namespace Engine::ECS
{
	class ISystem
	{
	public:
		virtual ~ISystem() = default;
	};
}
